<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Countdown to Hawaii" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Countdown to Hawaii" %>">
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div id="maximage">
      <%= image_tag('top.jpg', alt: "", width: 1024, height: 678) %>
    </div>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
       <%= render 'layouts/messages' %>
       <%= yield %>
    </main>
    <footer >
    <small>Photo by <a href="http://www.flickr.com/people/ornellas/">Justin Ornellas</a> / <a href="http://creativecommons.org/licenses/by/2.0/">CC BY</a></small></footer>
  </body>
</html>
